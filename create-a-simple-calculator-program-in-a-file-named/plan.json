{
    "plan": [
        "Create a file named `calculator.py`",
        "Write the following code in `calculator.py`:\n```python\ndef add(x, y):\n  return x + y\n```",
        "Create a directory named `tests`",
        "Create a file named `test_calculator.py` inside the `tests` directory",
        "Write the following code in `test_calculator.py`:\n```python\nimport unittest\nimport calculator\n\nclass TestCalculator(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(calculator.add(2, 3), 5)\n        self.assertEqual(calculator.add(-1, 1), 0)\n        self.assertEqual(calculator.add(0, 0), 0)\n\nif __name__ == '__main__':\n    unittest.main()\n```",
        "Run the tests using the command `python tests/test_calculator.py`"
    ]
}